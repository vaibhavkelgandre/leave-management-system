# 🔐 Role Capability Matrix — Who Can Do What

> **Single source of truth for authorization.** Companion to [`1.functional_requirements.md`](1.functional_requirements/README.md) and [`2.api_documentation.md`](2.api_documentation/README.md). Read this before wiring a new route, adding a nav item, or hiding/showing a button by role.
>
> 🚨 **Standing rule:** whenever a role gains, loses, or changes an ability — a new `requireRole(...)` gate, a changed row-level check in a service, a new `RequireRole`/`RoleGate` in the client, a new nav item — **update this document in the same change**. Same standing rule as [`2.api_documentation.md`](2.api_documentation/README.md) for endpoints and [`3.db.md`](3.db/README.md) for schema.

---

## 1. The four roles

| Role | How it's created | Position in the reporting tree | One-line summary |
|---|---|---|---|
| `SUPER_ADMIN` | **Singleton.** `POST /auth/register/hr` with `HR_REGISTRATION_CODE`. 409 if one already exists. | The true root. Can **never** have a manager. Profile is `VERIFIED` at creation. | Acts as HR **only for the `HR_ADMIN`s who report directly to it**. Company-wide read, deliberately narrow write. |
| `HR_ADMIN` | Invited by an existing `HR_ADMIN`/`SUPER_ADMIN` (`POST /users/invite`, `managerId` required). | Reports to another `HR_ADMIN` (whoever created them) or to `SUPER_ADMIN`. Root of its own branch. | Full HR authority **over its own reporting subtree**, plus company-wide read on a few list views. |
| `MANAGER` | Invited by HR (`managerId` optional). | Reports only to an `HR_ADMIN`. | Approves/rejects leave for **direct reports only**; can view their whole subtree; can delegate. |
| `EMPLOYEE` | Invited by HR (`managerId` required). | Reports to a `MANAGER` or an `HR_ADMIN`. | Self-service only — **unless** currently someone's active delegate. |

**Nobody can invite a `SUPER_ADMIN`** — the invite payload only accepts `EMPLOYEE`, `MANAGER`, `HR_ADMIN`.

### Reporting-line rules (enforced on every write that sets `manager_id`)

| Target role | Allowed manager roles |
|---|---|
| `EMPLOYEE` | `MANAGER`, `HR_ADMIN` |
| `MANAGER` | `HR_ADMIN` only (never another manager) |
| `HR_ADMIN` | `HR_ADMIN`, `SUPER_ADMIN` |
| `SUPER_ADMIN` | *none — never gets a manager* |

Reassignment is additionally rejected if it would create a reporting cycle.

---

## 2. The two questions every check answers

Authorization here is **never role alone**. There are two independent gates:

1. **Role gate** (route level, `requireRole(...)`) — "may this *kind* of user reach this endpoint at all?"
2. **Scope gate** (service level, row level) — "does this user have authority over *this specific person/record*?"

Scopes used throughout this document:

| Scope term | Means | Implemented by |
|---|---|---|
| **Self** | `actor.id === target.id` | inline checks / `/me/*` routes |
| **Direct reports** | one level down only | `findDirectReports` |
| **Subtree** | everyone transitively below, any depth | `isUserInSubtree` / `findSubtreeUsers` |
| **HR scope** | `HR_ADMIN` → own **subtree**. `SUPER_ADMIN` → **direct-report `HR_ADMIN`s only** | [`hrScopeService.js`](../server/src/services/hrScopeService.js) |
| **Creator ownership** | actor must be the target's `invited_by` | `changeManager` / `changeStatus` |
| **Delegate** | an active delegation window covers today | `isManagerOrDelegateOf` |
| **Company-wide** | every user, no scoping | `findAllUsers` / `findAllLeaveRequests` |

> ⚠️ **`SUPER_ADMIN` is not "HR_ADMIN with more power."** Its *read* access is company-wide like HR's, but every HR-scoped *write* reaches **only its direct-report `HR_ADMIN`s** — never those admins' own teams. Reusing subtree scoping for `SUPER_ADMIN` would silently mean "the entire company," which was explicitly rejected.

**Legend for the matrices below:** ✅ allowed · ⚠️ allowed but scoped (condition in the notes) · ❌ denied

---

## 3. Accounts, profile & self-service

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN | Notes |
|---|---|:--:|:--:|:--:|:--:|---|
| Bootstrap the top account | `POST /auth/register/hr` | — | — | — | — | Public + secret code. Creates the singleton `SUPER_ADMIN`. |
| Log in / out, Google login | `POST /auth/login`, `/google`, `/logout` | ✅ | ✅ | ✅ | ✅ | Public. |
| Read own session | `GET /auth/me` | ✅ | ✅ | ✅ | ✅ | |
| Accept an invite | `POST /auth/invitations/verify` `/accept` | ✅ | ✅ | ✅ | — | Public, token-scoped. |
| Forgot / reset password | `POST /auth/password-reset/*` | ✅ | ✅ | ✅ | ✅ | Public, token-scoped. |
| Edit **own** profile | `PATCH /users/me/profile` | ✅ | ✅ | ✅ | ✅ | Self only by construction (no `:id`). Field whitelist enforced twice. |
| Change **own** password | `POST /users/me/password` | ✅ | ✅ | ✅ | ✅ | Requires the current password. |
| Upload **own** onboarding documents | `POST /employees/me/documents/:type` | ✅ | ✅ | ✅ | ✅ | Self-upload only — there is **no** "HR uploads on an employee's behalf" path. |
| Add / view / delete **own** custom document | `POST\|GET\|DELETE /employees/me/documents/custom/*` | ✅ | ✅ | ✅ | ✅ | Delete is restricted to `OTHER`-type rows, so a required document can never be removed this way. |
| Submit **own** profile for verification | `POST /employees/me/profile/submit` | ✅ | ✅ | ✅ | ✅ | 400 until every required field and all four required documents exist, and until any **rejected** document has been replaced. |
| Own notifications (list / count / mark read) | `GET\|PATCH /notifications/*` | ✅ | ✅ | ✅ | ✅ | Always self-scoped; no role gate exists or is needed. |

---

## 4. Employee & org-structure management

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN | Notes |
|---|---|:--:|:--:|:--:|:--:|---|
| Invite a new user | `POST /users/invite` | ❌ | ❌ | ✅ | ✅ | Invitable roles: `EMPLOYEE`, `MANAGER`, `HR_ADMIN`. `managerId` required for `EMPLOYEE` and `HR_ADMIN`, optional for `MANAGER`. |
| Resend a pending invite | `POST /users/invite` (same endpoint) | ❌ | ❌ | ⚠️ creator **or** own subtree | ⚠️ creator **or** direct reports | Posting an email that already belongs to an `INVITED` user reissues its link (`200`, `reissued: true`) instead of failing. Row-level rule in `invitationService.reissueInvitation`: `invitations.invited_by === actor.id` **or** `isInActorsHrScope`, i.e. the same "creator or in-my-HR-scope" test as `PATCH /:id/manager`. Anyone else gets `409 This email already has a pending invitation` — deliberately **not** 403/404, since the caller asked to create an account for a taken address, and it reveals no more than the duplicate-email conflict it replaced. Scope is a downward walk, so an HR admin *above* the invitee qualifies only by being the creator. Reissue never changes `invited_by`, and ignores the name/role/`managerId` in the body. An address on an `ACTIVE`/`INACTIVE` account is a different answer: `409 An account with this email already exists`. |
| List users for a picker | `GET /users/options` | ⚠️ self only | ⚠️ own subtree | ✅ company-wide | ✅ company-wide | Same scoping as the row below, five columns wide. Sensitive fields aren't selected at all, so masking is moot. |
| List users | `GET /users` | ⚠️ self only | ⚠️ own subtree | ✅ company-wide | ✅ company-wide | No role gate — the **service** decides the slice. |
| View one user | `GET /users/:id` | ⚠️ self only | ⚠️ self + subtree | ✅ company-wide | ✅ company-wide | `requireUserScope`. Sensitive fields masked — see §9. |
| Count of own team | `GET /users/me/team/count` | ⚠️ 0 | ✅ subtree size | ✅ subtree size | ✅ subtree size | Count-only twin of the row below; same scope, one integer. |
| View own team list | `GET /users/me/team` | ⚠️ empty | ✅ subtree | ✅ subtree | ✅ subtree | No role gate; a user with no reports just gets `[]`. |
| Change who someone reports to | `PATCH /users/:id/manager` | ❌ | ❌ | ⚠️ | ⚠️ | HR-tier role gate **plus** either the actor is the target's `invited_by` **or** the target is inside the actor's HR scope. Plus hierarchy and cycle checks. |
| Activate / deactivate an account | `PATCH /users/:id/status` | ❌ | ❌ | ⚠️ | ⚠️ | Same creator-or-in-my-scope rule. You can never deactivate yourself. |
| Open an employee's full detail / verification record | `GET /employees/:id` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | Deliberately **stricter** than `GET /users/:id` — see §9. |

> 🔒 **Managing a person is scoped to your own branch, two ways in** (and the client gate is simply *HR-tier*, since these routes are HR-tier gated and My Team only ever lists the viewer's own subtree — a `MANAGER` is offered neither control). `PATCH /users/:id/manager` and `/status` each accept the actor if **either** they are the target's `invited_by` **or** the target sits inside their HR scope (an `HR_ADMIN`'s subtree; `SUPER_ADMIN`'s direct-report `HR_ADMIN`s only). Creator-alone was the original rule and was widened on direct request: it left an HR admin with no controls at all for anyone they'd inherited rather than invited — a colleague's joiner, or any account predating the invitation records — which read as a missing feature rather than a boundary.
>
> What still holds, and is the actual point of the check: **a different branch's people are unreachable**, since a subtree walk never goes sideways or upward. That also keeps `SUPER_ADMIN` un-deactivatable from below (nobody's subtree contains the root), which used to be a side effect of the "no `invited_by`" rule.
>
> 📊 **Reporting is the one place `SUPER_ADMIN`'s scope is wider, not narrower.** Its HR-*write* scope is direct-report `HR_ADMIN`s only — but `GET /leave-requests`, `/report` and `/report/csv` cover **every employee** for it (`reportableEmployeeIds` returns no restriction), because a role that can already read every request company-wide getting a leave report on three people was useless. `HR_ADMIN` stays subtree-scoped on all three.
>
> ⚖️ **Other company-wide views belong to `SUPER_ADMIN`, not HR.** Two pages/endpoints changed together on direct request: the **All Employees** roster (`/dashboard/employees`, client-gated) and **All Requests** (`GET /leave-requests/all`, 403 for HR). An `HR_ADMIN`'s view of both people and leave is their own branch — My Team and the team-scoped approvals list. Note the *endpoint* behind All Employees (`GET /users`) is deliberately **not** narrowed: HR still needs a company-wide user list for the manager pickers on the invite form and the payroll/slip filters. The restriction is on the page, and on every endpoint that would show another branch's records.

---

## 5. Leave types & holidays

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN |
|---|---|:--:|:--:|:--:|:--:|
| List / view leave types | `GET /leave-types`, `/leave-types/:id` | ✅ | ✅ | ✅ | ✅ |
| Include **inactive** leave types in the list | `GET /leave-types?includeInactive=true` | ❌ | ❌ | ✅ | ✅ |
| Create / edit a leave type | `POST /leave-types`, `PATCH /leave-types/:id` | ❌ | ❌ | ✅ | ✅ |
| Activate / deactivate a leave type | `PATCH /leave-types/:id/status` | ❌ | ❌ | ✅ | ✅ |
| View the holiday calendar | `GET /holidays` | ✅ | ✅ | ✅ | ✅ |
| Create / edit / delete a holiday | `POST\|PATCH\|DELETE /holidays` | ❌ | ❌ | ✅ | ✅ |

Leave types and holidays are **company-wide config** — the one area where HR authority is not subtree-scoped.

---

## 6. Leave requests

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN | Notes |
|---|---|:--:|:--:|:--:|:--:|---|
| Preview working days | `POST /leave-requests/preview` | ✅ | ✅ | ✅ | ✅ | Persists nothing. |
| Apply for leave | `POST /leave-requests` | ✅ | ✅ | ✅ | ⚠️ | **`SUPER_ADMIN`'s request is created directly as `APPROVED`** — no reviewer exists. All validation (balance, overlap, document requirement, working days) still applies. |
| Count of requests awaiting my decision | `GET /leave-requests/pending-count` | ⚠️ as delegate | ⚠️ own reports + delegated | ⚠️ where HR is the assigned manager | ⚠️ same | No role gate — scoped like `/team`, so anyone with nothing in scope gets `0`. |
| Who's on leave today | `GET /leave-requests/on-leave-today` | ⚠️ delegated teams only | ⚠️ own team | ⚠️ own branch | ✅ company-wide | No role gate; same scoping as `/team` except `SUPER_ADMIN`, which is company-wide. |
| Team / approvals queue | `GET /leave-requests/team` | ⚠️ delegated teams only | ⚠️ **direct reports** + delegated teams | ⚠️ HR scope | ⚠️ HR scope | **No role gate** — a plain employee acting as a delegate must reach this. A non-delegate employee gets `[]`, not 403. A manager gets direct reports only, **not** their full subtree. |
| View own requests | `GET /leave-requests/mine` | ✅ | ✅ | ✅ | ✅ | |
| Company-wide "All Requests" (read-only) | `GET /leave-requests/all` | ❌ | ❌ | ❌ **403** | ✅ | **`SUPER_ADMIN` only.** An `HR_ADMIN`'s view of leave is their own branch, which `/team` already returns — a scoped version of this endpoint would have been the same rows twice. |
| Filterable browse | `GET /leave-requests` | ❌ | ❌ | ⚠️ own subtree | ✅ company-wide | For HR, filtering for someone outside their subtree returns no rows. `SUPER_ADMIN` covers every employee — reporting is the one place its scope is *wider* than its HR-write scope, not narrower. |
| Leave-taken report + CSV | `GET /leave-requests/report`, `/report/csv` | ❌ | ❌ | ⚠️ own subtree | ✅ company-wide | Same split as the browse view above. |
| View one request / its audit trail / its document | `GET /leave-requests/:id`, `/:id/audit`, `/:id/document`, `/:id/document/download` | ⚠️ own | ⚠️ own + as manager/delegate | ⚠️ own subtree | ✅ company-wide | Denials are **404, never 403** — existence isn't leaked. `SUPER_ADMIN` stays company-wide so every row of the list above can be opened. |
| **Approve / reject** | `POST /leave-requests/:id/approve` `/reject` | ⚠️ only as an active delegate | ⚠️ only as the **assigned** manager, or a delegate for them | ⚠️ only when HR **is** the assigned manager | ⚠️ same | See the box below. |
| Withdraw / cancel | `POST /leave-requests/:id/withdraw` `/cancel` | ⚠️ own only | ⚠️ own only | ⚠️ own only | ⚠️ own only | Owner only; 404 for anyone else regardless of role. |
| **Override a decision** | `POST /leave-requests/:id/override` | ❌ | ❌ | ⚠️ own subtree | ❌ **403** | The one capability `HR_ADMIN` has and `SUPER_ADMIN` does not. |

> ⚖️ **HR has no approval bypass.** The flow is always: *employee submits → the actual assigned manager (or their active delegate) decides → HR may override afterwards.* HR can approve directly **only when HR genuinely is the assigned manager** — which already covers employees with no manager, every `MANAGER` (who only ever reports to an `HR_ADMIN`), and an `HR_ADMIN` reporting to another `HR_ADMIN`.
>
> A blocked HR-tier actor gets **403 with an explanatory message** when the employee is inside their HR scope (they legitimately know the record exists) and **404** when outside it. Nobody — not even HR — can act on **their own** request; that check runs before any role check.

### Delegation

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN | Notes |
|---|---|:--:|:--:|:--:|:--:|---|
| Nominate a delegate | `POST /delegations` | ❌ | ✅ | ❌ | ❌ | **Manager-only.** Not yourself; the delegate must be `ACTIVE`; overlapping windows rejected (409). |
| List delegations I created | `GET /delegations/mine` | ❌ | ✅ | ❌ | ❌ | |
| List delegations naming me | `GET /delegations/as-delegate` | ✅ | ✅ | ✅ | ✅ | Open to every role on purpose — **the delegate may be a plain `EMPLOYEE`** and is never told any other way. |

There is deliberately **no HR/admin view** of delegations.

---

## 7. Onboarding documents & profile verification

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN | Notes |
|---|---|:--:|:--:|:--:|:--:|---|
| Verification queue (`SUBMITTED` profiles) | `GET /employees/pending-verification` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | |
| Verified-employees list | `GET /employees/verified` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | |
| List someone's documents / get a signed URL | `GET /employees/:id/documents`, `/:id/documents/:type/url` | ⚠️ own | ❌ **404** | ⚠️ self-or-HR-scope | ⚠️ self-or-HR-scope | **A manager cannot see a report's onboarding paperwork** — unlike leave documents. |
| Open / download a document's bytes | `GET /employees/documents/:documentId/file` | ⚠️ own | ❌ **404** | ⚠️ self-or-HR-scope | ⚠️ self-or-HR-scope | Authorization comes from the document row's own `employee_id`, not a path param — same pattern as a salary slip's `/:id`. Serves required and custom documents alike. |
| Verify / reject a single document | `POST /employees/:id/documents/:type/review` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | Rejection deletes nothing; the employee re-uploads to replace. |
| Verify a profile | `POST /employees/:id/verify` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | `SUBMITTED` → `VERIFIED`, **and only once every required document is individually `VERIFIED`** — 400 while any is pending review or rejected. |
| Send a profile back | `POST /employees/:id/send-back` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | `reason` is mandatory. |

> 📌 **Leave documents and onboarding documents follow different rules.** Leave document: requester + approver + HR. Onboarding document: self + HR-in-scope only. A manager sees the first, never the second.

---

## 8. Salary structures & payroll

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN | Notes |
|---|---|:--:|:--:|:--:|:--:|---|
| View a salary structure | `GET /employees/:id/salary-structure` | ⚠️ own | ⚠️ own only | ⚠️ self-or-HR-scope | ⚠️ self-or-HR-scope | Own-structure visibility is deliberate payroll-readiness transparency. |
| Assign / edit a salary structure | `PATCH /employees/:id/salary-structure` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | |
| Preview a payroll run | `POST /salary-slips/calculate` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | The pay period must already be completed. Employees with no salary structure are skipped, not failed. |
| Confirm a payroll run | `POST /salary-slips/confirm` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | Recomputed server-side; never trusts the preview's figures. An already-`ACTIVE` period must be voided first. |
| View own payslips | `GET /salary-slips/mine` | ✅ | ✅ | ✅ | ✅ | Always self only, whatever the role. |
| View team payslips | `GET /salary-slips` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | |
| View / download one payslip | `GET /salary-slips/:id`, `/:id/pdf` | ⚠️ own | ❌ **404** | ⚠️ self-or-HR-scope | ⚠️ self-or-HR-scope | **A manager cannot see a report's payslip.** |
| Void a payslip | `POST /salary-slips/:id/void` | ❌ | ❌ | ⚠️ HR scope | ⚠️ HR scope | Not even the owning employee — voiding is HR's correction, never self-serve. 409 if already voided. |

### Leave balances

| Capability | Endpoint | EMPLOYEE | MANAGER | HR_ADMIN | SUPER_ADMIN |
|---|---|:--:|:--:|:--:|:--:|
| Own balances | `GET /leave-balances/me` | ✅ | ✅ | ✅ | ✅ |
| Someone else's balances | `GET /leave-balances/user/:id` | ⚠️ self only | ⚠️ self + subtree | ✅ company-wide | ✅ company-wide |

---

## 9. Field-level and view-level exceptions

The rules that don't fit a simple endpoint row:

1. **Sensitive profile fields are masked.** `pan_number`, `aadhar_number`, `passport_number`, `bank_account_number`, `bank_ifsc_code`, `bank_name` come back in full **only** to the employee themself and to HR-tier viewers. A manager viewing a direct report sees `null` for these. Other fields (phone, address, DOB, emergency contact, designation) are **not** masked — a manager plausibly needs a report's contact details.
2. **`GET /users/:id` is intentionally looser than `GET /employees/:id`.** The first lets any HR admin read any user company-wide (a directory lookup). The second is the verification/detail record and is HR-scope-restricted, matching every other HR *write* action.
3. **Visibility ≠ authority, everywhere.** `GET /leave-requests/all` is company-wide for HR while approving is not; a manager's team list spans their whole subtree while approval authority stops at direct reports.
4. **404 vs 403 is a deliberate choice.** 403 means "you can legitimately see this record, but this particular action isn't yours." 404 means "you have no business knowing this record exists." Preserve the distinction in any new check.
5. **`SUPER_ADMIN` bypasses its own workflows** because nobody sits above it: profile `VERIFIED` at creation, leave requests auto-`APPROVED` with an `AUTO_APPROVE` audit entry and no notification.

---

## 10. UI surfaces by role

Client-side gating is **UX only** — every endpoint is independently authorized server-side. Hiding a route hides nothing on its own.

| Route | Nav label | Who reaches it |
|---|---|---|
| `/dashboard` | Dashboard | everyone (tiles vary: delegation status → `MANAGER`; team overview → `MANAGER` and HR-tier) |
| `/dashboard/my-leave`, `/holidays`, `/salary-slips`, `/profile`, `/notifications` | My Leave, Leave Calendar, Salary Slips, … | everyone (HR-tier sees extra panels on the same pages: holiday CRUD, the team-slips tab, the payroll tool) |
| `/dashboard/my-leave/apply-leave` | — (reached from My Leave's "Request Leave" button) | everyone. **No sidebar entry** — applying is something you do from your own leave page, and the old modal version is gone |
| `/dashboard/documents/preview` | — | everyone; serves both self-view and HR review, gated by the signed-URL endpoints |
| `/dashboard/team` | My Team | `MANAGER`, `HR_ADMIN`, `SUPER_ADMIN`. For HR this is *the* people-management surface: per-person change-manager and activate/deactivate icons, plus the **Add Employee** action (a plain `MANAGER` sees neither — the icons need creator-or-scope, the invite action is HR-tier) |
| `/dashboard/approvals` | Approvals | `MANAGER`, `HR_ADMIN`, `SUPER_ADMIN`, **plus any user with an active delegation** (`alsoAllowIfActiveDelegate`). Override controls render for `HR_ADMIN` only; the read-only **All Requests** tab renders for `SUPER_ADMIN` only. HR gets one team-scoped list and no tabs. |
| `/dashboard/delegations` | Delegations | `MANAGER` only |
| `/dashboard/employees` | All Employees | **`SUPER_ADMIN` only** |
| `/dashboard/employees/new` | — (reached from All Employees, or from My Team for HR) | `HR_ADMIN`, `SUPER_ADMIN` — inviting is still an HR capability, so this keeps the wider gate; its "back" link points at My Team for HR and All Employees for `SUPER_ADMIN` |
| `/dashboard/team/:id` | — | `HR_ADMIN`, `SUPER_ADMIN` only (a manager may see the team *list* but not this detail page) |
| `/dashboard/leave-types` | Leave Types | `HR_ADMIN`, `SUPER_ADMIN` |
| `/dashboard/reports` | Reports | `HR_ADMIN`, `SUPER_ADMIN` |
| `/dashboard/profile-verification`, `/profile-verification/:id` | Profile Verification | `HR_ADMIN`, `SUPER_ADMIN` |
| `/dashboard/payroll-run` | — (reached from Salary Slips) | `HR_ADMIN`, `SUPER_ADMIN` |

---

## 11. Where each rule actually lives

| Concern | File |
|---|---|
| Route-level role gate | [`server/src/middlewares/requireRole.js`](../server/src/middlewares/requireRole.js) |
| Self / HR / manager-subtree gate for `:id` reads | [`server/src/middlewares/requireUserScope.js`](../server/src/middlewares/requireUserScope.js) |
| HR-tier scope (`HR_ADMIN` subtree vs `SUPER_ADMIN` direct reports) | [`server/src/services/hrScopeService.js`](../server/src/services/hrScopeService.js) |
| Reporting-hierarchy and cycle rules | [`server/src/services/reportingService.js`](../server/src/services/reportingService.js) |
| Who may approve / reject / override / withdraw a request | `resolveActingCapacity` in [`server/src/services/leaveRequestService.js`](../server/src/services/leaveRequestService.js) |
| Creator ownership for manager/status edits, sensitive-field masking | [`server/src/services/userService.js`](../server/src/services/userService.js) |
| Onboarding-document visibility | [`server/src/services/employeeDocumentService.js`](../server/src/services/employeeDocumentService.js) |
| Payroll and payslip authority | [`server/src/services/salarySlipService.js`](../server/src/services/salarySlipService.js), [`salaryStructureService.js`](../server/src/services/salaryStructureService.js) |
| Client route guards | [`client/src/components/routing/RequireRole.jsx`](../client/src/components/routing/RequireRole.jsx) |
| Client nav gating | [`client/src/components/layout/NavBar.jsx`](../client/src/components/layout/NavBar.jsx) |
| Client-side mirror of the approve/reject rule | [`client/src/utils/leaveRequestAuthz.js`](../client/src/utils/leaveRequestAuthz.js) |

---

## 12. Checklist when you change a role's abilities

1. Change the **route** gate (`requireRole` / `requireUserScope`) *and* the **service** row-level check — the service check is the real one; the route gate is convenience and belt-and-suspenders.
2. Decide **404 vs 403** for the denial and match §9.4.
3. Decide whether `SUPER_ADMIN` follows `HR_ADMIN` here, and whether its scope is direct-reports-only — don't reuse subtree scoping for it by reflex.
4. Update the client guard (`RequireRole` / `RoleGate` / `hasAnyRole`) and the nav entry so the UI never offers something the server will refuse.
5. Add or update tests for **both** the allowed and the denied role.
6. **Update this document** (the matrix row *and* any relevant note in §9), plus [`2.api_documentation.md`](2.api_documentation/README.md) if the endpoint's contract moved.
